
<!--Se esta utilizando un linear layout donde
        se especifica su orientazión a vertial
        android:orientation="vertical"-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    tools:context=".MainActivity">

    <!--Se crear un TextView que se le asigana un id
        txtTitulo para poder usarlo en la parte JAVA
        Por otro lado en su propiedad text
        hacer referencia a un recurso alternativo
        de los strings
        @string/titulo-->
    <TextView
        android:id="@+id/txtTitulo"
        android:text="@string/titulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <!--Se crear un TextView que se le asigana un id
        txtTablas para poder usarlo en la parte JAVA
        el cual mostrara las tablas-->
    <TextView
        android:id="@+id/txtTablas"
        android:layout_marginTop="15dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:layout_marginTop="15dp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Consultar"
        android:onClick="multiplicacion"/>

</LinearLayout>
